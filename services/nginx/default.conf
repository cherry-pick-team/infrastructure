upstream web {
    ip_hash;
    server api:5000;
}

server {
    location / {
        proxy_pass http://web/;
    }

    location /static {
        alias /etc/nginx/conf.d/static;
    }

    listen 80;
    server_name _;
}
